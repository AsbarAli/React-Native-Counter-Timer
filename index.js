/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {CustomCounterTimerContainer} from './src/containers/';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => CustomCounterTimerContainer);
